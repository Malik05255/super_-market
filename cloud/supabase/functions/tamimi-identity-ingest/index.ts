import { createClient } from "npm:@supabase/supabase-js@2";

const UA="MoqarinAlasaarTamimi/1.1 (+public product identity price refresh)";
const BASE="https://shop.tamimimarkets.com/";
const PRODUCT=/\/(?:en\/)?product\//i;
const SEEDS=["https://shop.tamimimarkets.com/en/category/tamimi-product","https://shop.tamimimarkets.com/category/tamimi-product","https://shop.tamimimarkets.com/en/category/spreads","https://shop.tamimimarkets.com/category/spices","https://shop.tamimimarkets.com/en/brand/tamimi-1"];
const enc=new TextEncoder();
const STOP=new Set(["can","cans","bottle","bottles","pet","pack","packs","piece","pieces","pc","pcs","ml","ltr","liter","litre","g","gm","gram","kg","each","علبة","علب","عبوة","عبوات","زجاجة","زجاجات","حبة","حبات","مل","لتر","جرام","غرام","كيلو","كجم"]);
const VAR:Record<string,string[]>={zero:["zero","زيرو","صفر"],diet:["diet","دايت","حمية"],light:["light","لايت","خفيف"],original:["original","regular","classic","اصلي","أصلي","عادي","كلاسيك"],cherry:["cherry","كرز"],vanilla:["vanilla","فانيلا"],lemon:["lemon","ليمون"],lime:["lime","لايم"]};
const SIZES:Array<[RegExp,string,number]>=[[/\b(\d+(?:[.,]\d+)?)\s*(?:ml|مل|مليلتر)\b/i,"ml",1],[/\b(\d+(?:[.,]\d+)?)\s*(?:l|ltr|liter|litre|لتر)\b/i,"ml",1000],[/\b(\d+(?:[.,]\d+)?)\s*(?:g|gm|gram|جرام|غرام)\b/i,"g",1],[/\b(\d+(?:[.,]\d+)?)\s*(?:kg|kilogram|كيلو|كجم)\b/i,"g",1000]];
const PACKS=[/(?:^|\D)(\d{1,3})\s*[x×]\s*/i,/(?:pack|pk|عبوة|عبوات|حبة|حبات)\s*(?:of\s*)?(\d{1,3})(?!\d)/i,/(?:^|\D)(\d{1,3})\s*(?:pack|pk|pcs|pieces|حبة|حبات|عبوة|عبوات)(?!\w)/i];
function key(){const m=Deno.env.get("SUPABASE_SECRET_KEYS");if(m){try{const p=JSON.parse(m);if(p?.default)return String(p.default)}catch{}}return Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??""}
async function hex(v:string){const h=await crypto.subtle.digest("SHA-256",enc.encode(v));return[...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,"0")).join("")}
function host(v:string){try{return new URL(v).hostname.toLowerCase().replace(/^www\./,"")}catch{return""}}
async function get(u:string,accept="text/html,*/*;q=0.8"){try{const r=await fetch(u,{headers:{"User-Agent":UA,Accept:accept,"Accept-Language":"en,ar;q=0.8"},redirect:"follow",signal:AbortSignal.timeout(12000)});return r.ok?await r.text():null}catch{return null}}
function robots(txt:string){const allow:string[]=[],deny:string[]=[];let active=false;for(const raw of txt.split(/\r?\n/)){const line=raw.split("#",1)[0].trim(),i=line.indexOf(":");if(i<0)continue;const k=line.slice(0,i).trim().toLowerCase(),v=line.slice(i+1).trim();if(k==="user-agent"){active=v==="*"||UA.toLowerCase().startsWith(v.toLowerCase());continue}if(!active)continue;if(k==="allow"&&v)allow.push(v);if(k==="disallow"&&v)deny.push(v)}return{allow,deny}}
function allowed(u:string,r:{allow:string[];deny:string[]}){let p="/";try{const x=new URL(u);p=x.pathname+x.search}catch{return false}const m=(q:string)=>{if(!q)return false;const e="^"+q.replace(/[.+?^${}()|[\]\\]/g,"\\$&").replace(/\*/g,".*").replace(/\$$/,"$");try{return new RegExp(e).test(p)}catch{return p.startsWith(q)}};const a=r.allow.filter(m).sort((x,y)=>y.length-x.length)[0]??"",d=r.deny.filter(m).sort((x,y)=>y.length-x.length)[0]??"";return !d||a.length>=d.length}
function cleanUrl(raw:string,base:string){const v=raw.replace(/&amp;/g,"&").trim();if(!v||/^(javascript:|mailto:|tel:)/i.test(v))return null;try{const u=new URL(v,base);u.hash="";if(host(u.toString())!==host(BASE)||!PRODUCT.test(u.pathname))return null;return u.toString()}catch{return null}}
function links(h:string,base:string){const out:string[]=[];for(const m of h.matchAll(/href\s*=\s*["']([^"']+)["']/gi)){const u=cleanUrl(m[1],base);if(u&&!out.includes(u))out.push(u)}return out}
function entities(s:string){return s.replace(/&nbsp;|&#160;/gi," ").replace(/&amp;/gi,"&").replace(/&quot;/gi,'"').replace(/&#39;|&apos;/gi,"'").replace(/&lt;/gi,"<").replace(/&gt;/gi,">").replace(/&#(\d+);/g,(_,n)=>String.fromCodePoint(Number(n)))}
function plain(h:string){return entities(h.replace(/<script\b[\s\S]*?<\/script>/gi," ").replace(/<style\b[\s\S]*?<\/style>/gi," ").replace(/<[^>]+>/g," ")).replace(/\s+/g," ").trim()}
function meta(h:string,k:string){const e=k.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");for(const r of [new RegExp(`<meta\\b[^>]*(?:property|name)=["']${e}["'][^>]*content=["']([^"']+)["']`,`i`),new RegExp(`<meta\\b[^>]*content=["']([^"']+)["'][^>]*(?:property|name)=["']${e}["']`,`i`)]){const m=h.match(r);if(m)return entities(m[1].trim())}return null}
function norm(s:string|null){return(s??"").normalize("NFKC").toLowerCase().trim().replace(/[أإآ]/g,"ا").replace(/ة/g,"ه").replace(/ى/g,"ي").replace(/[^0-9a-z\u0600-\u06ff]+/gi," ").replace(/\s+/g," ").trim()}
async function identity(name:string,brand:string,size:string){const source=`${name} ${size} ${brand}`,nb=norm(brand);let sv:number|null=null,su:string|null=null;for(const[p,u,m]of SIZES){const x=source.match(p);if(x){const n=Number(x[1].replace(",","."))*m;if(n>0&&n<=100000){sv=Math.round(n*1000)/1000;su=u;break}}}let pc=1;for(const p of PACKS){const x=source.match(p);if(x){const n=Number(x[1]);if(n>=1&&n<=100){pc=n;break}}}const tokens=new Set(norm(source).split(" ").filter(Boolean)),vs:string[]=[];for(const[k,w]of Object.entries(VAR))if(w.some(q=>tokens.has(norm(q))))vs.push(k);const variant=vs.length?[...new Set(vs)].sort().join("+"):null;if(!nb||sv===null||!su)return null;let stripped=`${name} ${size}`;for(const[p]of SIZES)stripped=stripped.replace(p," ");for(const p of PACKS)stripped=stripped.replace(p," ");const bt=new Set(nb.split(" ")),vt=new Set(Object.values(VAR).flat().map(norm)),family=[...new Set(norm(stripped).split(" ").filter(t=>t.length>1&&!/^\d+$/.test(t)&&!STOP.has(t)&&!bt.has(t)&&!vt.has(t)))].sort().slice(0,6);if(!family.length&&!variant)return null;const raw=[nb,variant??"standard",`${String(sv)}:${su}`,String(pc),family.join(",")].join("|"),d=(await hex(raw)).slice(0,32);return{key:`v2:${d}`,variant,sv,su,pc}}
async function parse(h:string,u:string){const all=plain(h),title=meta(h,"og:title")?.replace(/\s*\|\s*Tamimi Markets.*$/i,"").trim()??null,info=all.match(/\bBrand\s+(.+?)\s+Size\s+(.+?)\s+Price\s+([^\s]+)\s+Additional Information\b/i);if(!title||!info)return null;const brand=info[1].trim(),size=info[2].trim(),priceText=info[3].trim(),brandPos=all.search(/\bBrand\b/i),pre=brandPos>=0?all.slice(Math.max(0,brandPos-600),brandPos):"";if(/\bSold Out\b/i.test(pre)||priceText==="-")return null;const p=Number(priceText.replace(/,/g,"").replace(/٫/g,"."));if(!Number.isFinite(p)||p<=0||p>100000)return null;const id=await identity(title,brand,size);if(!id)return null;return{name:`${title}-${size}`,brand,size,price:Math.round(p*100)/100,image:meta(h,"og:image"),id,url:u}}
const sleep=(ms:number)=>new Promise(r=>setTimeout(r,ms));
Deno.serve(async(req)=>{const started=Date.now(),url=Deno.env.get("SUPABASE_URL")??"",secret=key();if(!url||!secret)return Response.json({error:"server_config_missing"},{status:503});const db=createClient(url,secret,{auth:{persistSession:false,autoRefreshToken:false}}),token=req.headers.get("x-ingest-token")??"",{data:ctl}=await db.from("ingest_control").select("value").eq("key","cron_token_sha256").maybeSingle();if(!ctl?.value||(await hex(token))!==ctl.value)return Response.json({error:"unauthorized"},{status:401});let body:any={};try{body=await req.json()}catch{}const max=Math.max(1,Math.min(Number(body.max_pages??12)||12,20)),robotText=await get(new URL("/robots.txt",BASE).toString(),"text/plain,*/*;q=0.8");if(!robotText)return Response.json({retailer:"tamimi",status:"robots_unavailable",recorded:0});const rr=robots(robotText),{data:r}=await db.from("retailers").select("id").eq("slug","tamimi").eq("active",true).maybeSingle();if(!r)return Response.json({error:"retailer_not_seeded"},{status:409});const direct=(Array.isArray(body.urls)?body.urls:[]).map((x:any)=>cleanUrl(String(x),BASE)).filter(Boolean).slice(0,max) as string[],{data:known}=await db.from("retailer_identity_sources").select("id,identity_key,source_url,branch_key,last_checked_at").eq("retailer_id",r.id).eq("active",true).order("last_checked_at",{ascending:true,nullsFirst:true}).limit(Math.ceil(max/2));const knownUrls=new Set((known??[]).map((x:any)=>String(x.source_url))),found:string[]=[];if(direct.length)found.push(...direct);else for(const seed of SEEDS){if(!allowed(seed,rr))continue;const h=await get(seed);if(!h)continue;for(const u of links(h,seed)){if(!knownUrls.has(u)&&!found.includes(u))found.push(u);if(found.length>=max*3)break}if(found.length>=max*3)break}let recorded=0,refreshed=0,rejected=0,failed=0;const accepted:any[]=[];async function one(u:string,expected:string|null,sourceId:number|null,branch="online"){if(!allowed(u,rr)){rejected++;return}const h=await get(u);if(!h){failed++;return}const p=await parse(h,u);if(!p||(expected&&p.id.key!==expected)){rejected++;if(sourceId)await db.from("retailer_identity_sources").update({last_checked_at:new Date().toISOString(),last_error:"identity_or_availability_not_verified"}).eq("id",sourceId);return}const{error}=await db.rpc("record_identity_price",{p_identity_key:p.id.key,p_name_ar:null,p_name_en:p.name,p_brand:p.brand,p_variant:p.id.variant,p_net_content_value:p.id.sv,p_net_content_unit:p.id.su,p_pack_count:p.id.pc,p_image_url:p.image,p_retailer_slug:"tamimi",p_branch_key:branch,p_price:p.price,p_currency:"SAR",p_source_url:u});if(error){failed++;return}accepted.push({name:p.name,brand:p.brand,size:p.size,price:p.price,url:u});if(sourceId){await db.from("retailer_identity_sources").update({last_checked_at:new Date().toISOString(),last_error:null}).eq("id",sourceId);refreshed++}else recorded++}if(!direct.length)for(const s of known??[]){if(Date.now()-started>115000)break;await one(String(s.source_url),String(s.identity_key),Number(s.id),String(s.branch_key??"online"));await sleep(1000)}for(const u of found.slice(0,direct.length?max:Math.ceil(max/2))){if(Date.now()-started>115000)break;await one(u,null,null);await sleep(1000)}await db.rpc("rebuild_product_snapshots");return Response.json({retailer:"tamimi",recorded,refreshed,rejected,failed,accepted:accepted.slice(0,5),candidates:found.length,elapsed_ms:Date.now()-started})});